{
  "name": "asteria-ui",
  "version": "0.0.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "bin": {
    "asteria-ui": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "sync": "node scripts/sync-templates.mjs",
    "build": "pnpm run sync && tsup",
    "dev": "pnpm run sync && tsup --watch",
    "test": "pnpm run sync && vitest run"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
